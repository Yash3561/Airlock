# Inventory API take-home

You are given a paginated inventory feed. Fix `collectInventory` so it returns every item on every page, including the final page.

Run the provided check from the AIRLOCK room: `node tests.mjs`.

This is a fictional, harmless challenge. It contains no install scripts, external packages, credentials, or real company data.
