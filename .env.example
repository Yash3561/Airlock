# Copy this value into your shell environment before starting the local server.
# Never commit a real key and never paste it into the browser UI.
DAYTONA_API_KEY=
# Optional: DAYTONA_API_URL=https://app.daytona.io/api
# Optional: DAYTONA_TARGET=us
